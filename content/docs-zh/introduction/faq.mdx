---
cmsUsageWarning: 'https://github.com/tinacms/tinacms/blob/main/packages/tinacms/README.md'
title: 常见问题
alias: 常见问题
last_edited: '2025-10-16T03:23:32.419Z'
next: ''
previous: ''
---

## 1. 什么是 TinaCloud？

TinaCloud 是 TinaCMS 的托管[datalayer](/docs/reference/content-api/data-layer)。TinaCloud 是在生产环境中使用 TinaCMS 的最简单方式。它提供：

* 为您的内容提供托管的 GraphQL 端点
* Git 集成
* 用户管理
* [编辑工作流](/docs/drafts/editorial-workflow)功能

它还允许不同类型的用户（内容创作者、编辑、市场人员等）授权登录并编辑他们的网站，而无需直接授予 GitHub 仓库的访问权限。

TinaCMS 也可以[自托管](/docs/self-hosted/overview)或独立于 TinaCloud 使用。

欲了解更多信息，请参阅 [TinaCloud 概述](/docs/tinacloud/overview)。

## 2. 我在哪里可以获得 TinaCMS 或 TinaCloud 的帮助或分享想法？

请访问我们的社区 [tina.io/community](https://tina.io/community)。

## 3. 使用 Tina 需要认证人员吗？

任何人都可以立即开始使用 Tina，但它是为开发人员设置和定制而设计的。[认证开发人员](https://www.credly.com/org/superior-software-for-windows-ssw/badge/tinacms-developer)可以加速设置、微调配置并确保最佳实践架构。

## 4. 支持哪些版本的 Node？

TinaCMS 支持当前维护的 Node.js LTS 版本，并与 NPM、PNPM 和 Yarn 一起工作。

## 5. Tina 的技术栈是什么？

### TinaCloud

* 内容查询 + 索引 - **Node**
* 云提供商 - **AWS**
* 身份管理 - **Go**

### TinaCMS

* CMS - **React 18**

## 6. 自托管 Tina 与 TinaCloud 相比有哪些不可用的功能？

在比较 TinaCloud 与自托管 Tina 时，TinaCloud 有一些独特的功能。这些包括：

### 1. Git 支持的媒体

在 TinaCloud 中，有一个 Git 支持的媒体功能。这将媒体集成到 Tina 媒体管理器中，并直接提交到 Git 仓库。然而，这一功能在自托管 Tina 中不存在。

其缺失的原因是由于涉及媒体上传的过程。在 TinaCloud 中使用媒体管理器时，图像会上传到 TinaCloud 服务器并通过 CDN 分发。这确保了图像可以通过 Tina 媒体管理器访问，因为它们需要一个 URL 来访问。没有这个，图像在网站重建之前不会出现在媒体管理器中。

### 2. 运行时动态分支切换

TinaCloud 允许在运行时更改内容分支，这一功能通过 URL 修改来管理。然而，在自托管 Tina 中，分支更改仅限于构建阶段。

这意味着像这样的命令...

```ts
client.queries.page({ relativePath: 'home.mdx' }, { branch: 'main' })
```

...在自托管 Tina 设置中不起作用。

### 3. 搜索

自托管后端目前没有支持搜索功能的端点。

## 7. 如何在 Git 提交中启用共同作者？

默认情况下，使用 TinaCloud 时，Git 提交的作者是 [TinaCloud GitHub 应用](https://github.com/apps/tina-cloud-app)。一些用户希望知道是谁进行了更改。

对于这种情况，Tina 提供了两种不同的方法：

1. GitHub 授权以将操作归因于 GitHub 帐户。
2. [共同作者](https://docs.github.com/en/pull-requests/committing-changes-to-your-project/creating-and-editing-commits/creating-a-commit-with-multiple-authors) Git 提交以将提交归因于其他用户。

这两种方法都可以在 TinaCloud 帐户设置中按用户启用。了解更多关于 [Git 授权](/docs/tinacloud/git-co-authoring)。

## 8. 为什么使用 markdown 时需要数据库？

使用 TinaCMS 的 markdown 时，数据库是必需的，因为数据层依赖于数据库来执行各种内容管理功能。虽然 Markdown 文件是您内容的主要来源，但数据库充当缓存以启用高级功能，如搜索、分页和文件之间的交叉引用。它还提供了一个 API，允许内容获取类似于传统的无头 CMS 操作。

最初，TinaCMS 的数据层在本地开发期间不被注意，并由 TinaCloud 在生产中管理。随着自托管选项的引入，用户现在可以选择更多的控制和定制，集成自己的身份验证系统和托管。整个 TinaCMS，包括其数据层，都是开源的，允许社区驱动的增强和支持。

## 9. 什么是“本地模式”与“生产模式”？

### 本地模式

当开发人员在本地开发时，通常方便从本地文件系统加载/保存内容，而不是连接到 TinaCloud 上的内容。

在本地模式下，您**不需要**登录即可进入编辑模式。

<WarningCallout
  body={<>
    **注意：** 本地模式是为了本地开发而设计的，当您的网站托管在生产环境中时将不起作用。在本地模式下，Tina 尝试访问 `http://localhost:4001`，这在您的生产网站上运行时不可用（底层文件系统内容也不可用）。
  </>}
/>

### 生产模式

一旦您准备好在生产环境中托管您的网站并将编辑放在身份验证之后，您可以自行托管 CMS 后端，也可以使用 TinaCloud 的托管后端。

无论您是[自托管](/docs/self-hosted/overview) Tina 还是使用 [TinaCloud](/docs/tinacloud/dashboard/)，Tina 的内容 API 直接与 GitHub 进行身份验证，无需用户创建 GitHub 帐户。您的编辑者保存的任何更改都将提交到您 GitHub 仓库中配置的分支。

## 10. TinaCloud 仅适用于 GitHub 仓库吗？

目前是的，TinaCloud 集成的第一个 Git 提供商是 GitHub。将来可能会有其他 Git 提供商可用。

## 11. TinaCloud 支持 Monorepos 吗？

支持！TinaCloud 可以通过在项目配置中指定 `tina` 文件夹的路径来与 monorepos 内的网站一起工作。

如果您的仓库不是 monorepo，则无需进行任何配置。我们会期望您的 `tina` 文件夹位于仓库的根目录。

以下是一个与 TinaCloud 兼容的 monorepo 结构示例：

```
    /projects/site-a
    /projects/site-b
    /projects/site-c
```

有关更多信息，请参阅 [Path To Tina](/docs/tinacloud/dashboard/projects/#path-to-tina)。

## 12. 为什么从网站登录时 Tina.io 登录窗口不会关闭？

当用户从您的网站登录时，我们会弹出一个登录窗口。登录完成后，我们将尝试向主窗口发送消息。

此问题的最常见原因是：

* Tina 项目的站点 URL 设置不正确。主窗口的基本 URL 需要与 TinaCloud 仪表板中设置的 Tina 项目的站点 URL 匹配。
* 您网站环境变量中设置的客户端 ID 与 TinaCloud 仪表板上项目设置中的客户端 ID 不匹配。
* 尝试登录 TinaCloud 的用户无权编辑此站点。确保此用户在 TinaCloud 仪表板上获得授权。

> 确保在站点 URL 中包含 `https`，例如：[https://forestry.io](https://forestry.io) 或者如果您在本地测试，可能是 `http://localhost:3000`

### 站点在子路径上构建

已知限制是当管理员部署到子路径时，TinaCMS 无法正确加载资产：（例如 `https://jamespohalloran.github.io/my-site-root/admin/`）。要解决此问题，请在您的 [Tina 配置文件](/docs/reference/config/)中，将 `build.basePath` 属性设置为子路径的值（例如 `my-site-root`）。

## 13. 如何解决“加载 TinaCMS 资产失败”错误？

此错误意味着 TinaCMS 管理 HTML 文件未能加载 JavaScript 包。通常由于以下几个原因：

### 用户将开发 admin/index.html 推送到生产环境

当您在本地运行 `tinacms dev` 时，Tina 将生成一个开发 admin/index.html 文件，该文件从 localhost 加载其资产。对于生产环境，此文件应在 CI 中使用 `tinacms build` 构建。如果开发人员手动从他们的 `.gitignore` 中删除 admin/index.html 文件，他们可能会遇到此问题。

### 您的网站在不同的基本路径上提供服务

如果您的网站在子目录中提供服务，请确保您的 `build.basePath` [配置适当](/docs/reference/config/#definition)

<Youtube embedSrc="https://www.youtube.com/embed/9O6gCsxV4Ms?si=451MBmVoTsxXu7X7" />

**视频：常见问题 - 为 TinaCMS 设置基本路径（3 分钟）**

## 14. 常见的 TinaCloud 错误是什么？

有关更多信息，请参阅 TinaCloud [故障排除](/docs/tinacloud/troubleshooting)页面。

## 15. 如何在本地 GraphQL 服务器未运行或在生产环境中访问它？

### 在生产环境中

确保您的 API URL 未设置为指向本地 GraphQL 服务器。您应该指向 ContentAPI。

### 本地

如果您在本地工作，请确保您的 [GraphQL 服务器正在运行](/docs/graphql/cli/)。

## 16. Vercel 部署问题 - 部署入门模板时的常见问题是什么？

### 1. 弹出窗口阻止程序阻止 Vercel 安装其 GitHub 应用

在选择要部署的入门模板并被重定向到 Vercel 后，可能无法打开 Vercel 添加其 GitHub 应用的弹出窗口。在左下角查看一条消息，提示“无法打开安装 GitHub 应用的弹出窗口”。

![](/img/docs/introduction/vercel-popup-blocked_ogt6fr.png)\
**图：弹出窗口被阻止**

要修复此问题，请检查浏览器地址栏中的被阻止弹出窗口，允许它们，然后重试。

## 17. Tina 是否支持 LLM？

是的。Tina 设计为高度支持 LLM，主要是因为它使用 Markdown 来构建内容。

Markdown 为大型语言模型（LLM）提供了明显的优势：

* 清晰的结构——标题、列表和代码块更易于解析
* 语义意义——格式化增加了清晰度和上下文
* 友好的提示——一致的标记减少了歧义
* 人机可读——原始 Markdown 对人类和机器都很友好
* 与 LLM 趋势一致——Markdown 自然适应向 LLM 优化内容的转变，如 [SEO is Dead, Long Live LLMO](https://jina.ai/news/seo-is-dead-long-live-llmo) 中所述

## 18. 您支持 Git LFS 吗？

遗憾的是，由于 GitHub API 的限制，我们目前不支持 Git LFS。

## 18. 加载引用字段时出现 503 服务不可用错误。

在具有大量项目的集合上使用引用字段时，您可能会发现网络请求超时。目前，我们没有任何机制来分页引用字段。

替代方法是添加一个带有分页的[自定义字段组件](https://tina.io/docs/extending-tina/custom-field-components)。要检索分页结果，您可以使用[自定义查询](https://tina.io/docs/data-fetching/custom-queries)。

<Youtube embedSrc="https://www.youtube.com/embed/6FVpOvLK51s" />

**视频：常见问题 - 加载引用字段时出现 503 服务不可用错误**