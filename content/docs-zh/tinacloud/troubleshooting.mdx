---
title: TinaCloud 故障排除
alias: troubleshooting
last_edited: '2026-06-23T01:33:53.801Z'
next: content/docs-zh/tinacloud/dashboard.mdx
previous: content/docs-zh/tinacloud/network-requirements.mdx
---

## TinaCloud 常见错误有哪些？

<accordionBlock
  accordionItems={[
    {
      heading:
        "如何解决 \"无法找到记录: 'tina/__generated__/_graphql.json'\" 错误？",
      docText:
        "当 TinaCloud 的 GraphQL API 无法在您的 GitHub 仓库中找到文件时，会返回此错误。这可能在以下情况下发生：\n\n* `tina` 文件夹（以及 `__generated__` 子文件夹）不在您的 GitHub 仓库远程中\n  * 如果文件夹在您的本地仓库中，但不在远程中，请确保没有 `.gitignore` 文件排除它\n* Tina 配置了一个不存在的分支或不包含 `tina` 文件夹的分支\n  * 应创建引用的分支，并应包含 `tina` 文件夹\n* TinaCMS 组件上的 apiURL 属性配置错误\n  * 检查 apiURL，确保其格式为 `https://content.tinajs.io/content/{tina_client_id}/github/{branch}`，其中 `{tina_client_id}` 与 TinaCloud 项目中的客户端 ID 匹配，`{branch}` 是有效的分支\n"
    },
    {
      heading:
        '如何解决 "本地 GraphQL 模式与远程 GraphQL 模式不匹配" 错误？',
      docText:
        "如果您在构建日志中收到此错误，则表示您部署的网站中的 `tina/tina-lock.json` 与 TinaCloud 中的版本不匹配。要解决此问题：\n\n1. 确保您的模式（从 `tina/config.ts` 生成）不包含任何变化的信息，如时间戳或随机文本\n2. 确保您的项目中有最新版本的 `@tinacms/cli` 和 `tinacms`\n3. 在本地运行开发命令\n4. 提交对 `tina/tina-lock.json` 的任何更改，并将其推送到 TinaCloud 中链接的 git 仓库\n\n如果您在访问 TinaCMS 界面时收到此错误，可能是项目中 `tinacms` 和 `@tinacms/cli` 的版本不匹配导致的。更新这两个依赖项到最新版本，并在本地运行开发命令。提交对 `tina/tina-lock.json` 的任何更改，并将其推送到 TinaCloud 中链接的 git 仓库。\n"
    },
    {
      heading: "如何解决由未索引的分支引起的错误？",
      docText:
        "如果您收到类似 `指定的分支 'my-branch-name' 未被 TinaCloud 索引` 的错误，或根本没有看到任何分支被索引，请尝试以下操作：\n\n1. **确保 `tina/tina-lock.json` 存在并已提交到您的仓库。** 这是分支未被索引的最常见原因。`tina-lock.json` 文件是在您本地运行 `tinacms dev` 时生成的——它**不是**仅通过 `tinacms build` 生成的。运行 `tinacms dev`（或您的包装开发命令，如 `yarn dev`），然后提交并推送生成的 `tina/tina-lock.json` 文件。有关更多详细信息，请参阅[将 Tina 添加到现有站点](/docs/introduction/tina-init/)。\n2. 验证默认分支上是否存在 `tina/config.ts` 配置文件\n3. 验证在传递给 `tina/config.ts` 中 defineConfig 的配置属性中指定了正确的分支。请注意，此值可能在您的 CI 构建过程中设置为环境变量\n4. 验证分支是否存在于 GitHub 仓库中\n5. 将分支拉入您的 TinaCloud 项目中。**视频：** [为什么 TinaCloud 与 GitHub 配合得如此好 | TinaCMS 数据层 | Eli Kent (3 分钟)](https://youtube.com/embed/TatnNqKPKIQ?start=109)\n6. 您还可以通过在该分支的 `tina/tina-lock.json` 文件中进行空白更改、提交并推送到 GitHub 来强制 TinaCloud 重新索引分支。这将启动分支的索引，几分钟后错误应得到解决\n7. 确保 Tina 项目位于根目录中\n"
    },
    {
      heading:
        '如何解决 "保存文档时出现问题" 错误？',
      docText:
        "在不使用 [编辑工作流](/editorial-workflow) 的情况下使用 TinaCloud，您需要确保 [TinaCloud 应用](https://github.com/apps/tina-cloud-app) 能够提交到仓库的选定分支。\n\n如果不能，您将看到如下形式的错误：\n\n```text\nTina 在更新页面时捕获到错误：\n\n错误：无法获取，错误：\n    PUT src/pages/some-page.md 时出错\n```\n\n**视频：** [常见问题 - 使用 TinaCMS 绕过 GitHub 分支保护 (3 分钟)](https://www.youtube.com/embed/MTIJotdIG9E?si=8mcaNAqogQRwL678)\n\n通常这种情况发生在您的主分支上有分支保护，而 TinaCloud 无法绕过它。在 2024 年 7 月之前，TinaCloud GitHub 应用对仓库具有管理员权限，这使得它可以绕过大多数仓库的这些规则。\n\n要解决此问题，可以：\n\n* 从传统分支保护升级到分支规则集 - [https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/creating-rulesets-for-a-repository](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/creating-rulesets-for-a-repository)\\\n  确保允许 TinaCloud 应用绕过需要分支保护的规则\n* 启用 [编辑工作流](/editorial-workflow) 在分支上创建 PR\n"
    },
    {
      heading: '如何解决 "无效或未定义的分支" 错误？',
      docText:
        "Tina 当前使用的分支无效或未定义。请仔细检查是否选择了正确的分支，并且该分支确实存在。\n\n您可以通过访问 `https://app.tina.io/projects/<your-project-id>/configuration` 查看 TinaCloud 已索引的分支。\n\n![已索引的分支](/img/docs/tinacloud/branch-status.png)\\\n**图：TinaCloud 索引的分支**\n"
    },
    {
      heading: '如何解决 "tina 目录未推送到 git" 错误？',
      docText:
        "`tina` 目录中的所有内容（除了 `__generated__` 文件夹）都需要推送到 git 仓库。请确保将其添加到您的 git 仓库中（并确保您**没有**在 `.gitignore` 中列出它）。还要确保您尝试使用的分支具有 `tina` 目录并且是最新的。\n"
    },
    {
      heading: '如何解决 "API URL 格式错误" 错误？',
      docText:
        "ContentAPI URL 格式不正确。请参阅我们的 [TinaCloud URL 格式指南](/docs/tinacloud/overview/#enabling-tina-cloud-in-tinacms) 了解 URL 应如何格式化。\n"
    },
    {
      heading: '如何解决 "无效的站点 URL" 错误？',
      docText:
        "登录 TinaCloud 时，您尝试登录的网站的 URL 会通过 `?origin=` URL 参数发送。当身份验证窗口的 `?origin=` URL 参数中指定的 URL 与您的 TinaCloud 站点 URL 中定义的不匹配时，可能会发生此错误。\n\n要解决此问题，请确保您的 TinaCloud 站点 URL 设置中指定的一个 URL 与 `?origin=` URL 参数中显示的 URL 完全匹配。\n\n!['?origin=' URL 参数](/img/docs/introduction/origin-url-highlight.png)\\\n**图：URL 参数**\n\n![TinaCloud 站点 URL 设置](/img/docs/introduction/tina-site-url-highlight.png)\\\n**图：TinaCloud 站点 URL 设置**\n"
    },
    {
      heading: "编辑工作流卡在更新分支上？",
      docText:
        "有时编辑工作流会卡在更新分支步骤。当您看到这种情况时，请确保项目设置为正确的仓库。\n\n![TinaCloud 仓库设置](/img/docs/introduction/repository-tinacloud.png)\\\n**图：TinaCloud 仓库设置**\n"
    },
    {
      heading:
        '如何解决在 VPN 或受限网络上 "登录尝试超时" 错误？',
      docText:
        "TinaCloud 登录流程通过多个外部域（AWS Cognito、API Gateway、GitHub）重定向，这些域可能会被您的 VPN 或防火墙阻止。如果重定向链中的任何域被阻止，登录将在完成前超时。\n\n要解决此问题，请确保您的网络允许出站流量到 [网络要求](/docs/tinacloud/network-requirements/) 页面上列出的所有域。仅将 `*.tina.io` 列入白名单是不够的——身份验证流程还需要访问 AWS 和 GitHub 域。\n\n要诊断哪个域被阻止，请打开浏览器的开发者工具（网络选项卡）并尝试登录。查找任何停滞或失败的请求。\n"
    }
  ]}
  fullWidth={true}
/>