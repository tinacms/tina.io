---
id: /docs/extending-tina/custom-field-components
title: Custom Input Components
last_edited: '2025-03-17T00:34:57.460Z'
next: content/docs/extending-tina/customize-list-ui.mdx
previous: ''
---

You can use a custom React component in place of a usual field type input.

Provide your new component through the `ui.component` property of the field.

## The `ui.component` Property

This should be set to a valid React component that accepts three props:

- `field`: The [field definition](https://tinacms.org/docs/reference/toolkit/fields) for the current field.
- `input`: The data and callbacks necessary to make an input.
- `meta`: Metadata about the field in the form. (e.g. `dirty`, `valid`)

> Checkout the [react-final-form](https://github.com/final-form/react-final-form#fieldrenderprops) docs for a more detailed description of the `input` and `meta` props.

## Built-In Components

Tina also provides a variety of pre-built components you can use via the `ui.component` property. Set it to a string with the plugin name.

```javascript
{
  type: 'string',
  label: 'Background Color',
  name: 'color',
  ui: {
    component: "color",
    colorFormat: "rgb"
  }
}
```

> You can pass in additional plugin configurations via `ui` – such as `colorFormat` in the above example.

### Default Field Plugins

<ImageAndText
  image="/img/fields/text.png"
  docText={
    <>
      #### Text Input Plugin The default input type for string fields. Set
      `ui.compontent` to `"text"`. [Plugin
      details](/docs/reference/toolkit/fields/text/).
    </>
  }
/>

<ImageAndText
  image="/img/fields/textarea.png"
  docText={
    <>
      #### Textarea Input Plugin Used for multi-line text input. Set
      `ui.compontent` to `"textarea"`. [Plugin
      details](/docs/reference/toolkit/fields/textarea/).
    </>
  }
/>

<ImageAndText
  image="/img/fields/number-field.png"
  docText={
    <>
      #### Number Input Plugin Used for numerical values. Set `ui.compontent` to
      `"number"`. [Plugin details](/docs/reference/toolkit/fields/number/).
    </>
  }
/>

<ImageAndText
  image="/img/fields/image.png"
  docText={
    <>
      #### Image Input Plugin Used for uploading and selecting images. Set
      `ui.compontent` to `"image"`. [Plugin
      details](/docs/reference/toolkit/fields/image/).
    </>
  }
/>

<ImageAndText
  image="/img/fields/color.jpg"
  docText={
    <>
      #### Color Input Plugin Used for color selection with a color picker. Set
      `ui.compontent` to `"color"`. [Plugin
      details](/docs/reference/toolkit/fields/color/).
    </>
  }
/>

<ImageAndText
  image="/img/fields/toggle.png"
  docText={
    <>
      #### Toggle Input Plugin Used for boolean values with an on/off switch.
      Set `ui.compontent` to `"toggle"`. [Plugin
      details](/docs/reference/toolkit/fields/toggle/).
    </>
  }
/>

<ImageAndText
  image="/img/fields/radio-group-field-horizontal-radio.gif"
  docText={
    <>
      #### Radio Group Input Plugin Used for selecting a single option from a
      list of radio buttons. Set `ui.compontent` to `"radio-group"`. [Plugin
      details](/docs/reference/toolkit/fields/radio-group/).
    </>
  }
/>

<ImageAndText
  image="/img/fields/select-field.png"
  docText={
    <>
      #### Select Input Plugin Used for selecting from a dropdown menu of
      options. Set `ui.compontent` to `"select"`. [Plugin
      details](/docs/reference/toolkit/fields/select/).
    </>
  }
/>

<ImageAndText
  image="/img/fields/tags-field.png"
  docText={
    <>
      #### Tags Input Plugin Used for creating and managing multiple tag values.
      Set `ui.compontent` to `"tags"`. [Plugin
      details](/docs/reference/toolkit/fields/tags/).
    </>
  }
/>

<ImageAndText
  image="/img/fields/list-field.png"
  docText={
    <>
      #### List Input Plugin Used for managing an array of primitive values. Set
      `ui.compontent` to `"list"`. [Plugin
      details](/docs/reference/toolkit/fields/list/).
    </>
  }
/>

<ImageAndText
  image="/img/fields/group.gif"
  docText={
    <>
      #### Group Input Plugin Used for grouping related fields together. Set
      `ui.compontent` to `"group"`. [Plugin
      details](/docs/reference/toolkit/fields/group/).
    </>
  }
/>

<ImageAndText
  image="/img/blocks.gif"
  docText={
    <>
      #### Group List Input Plugin Used for managing an array of objects with
      the same shape. Set `ui.compontent` to `"group-list"`. [Plugin
      details](/docs/reference/toolkit/fields/group-list/).
    </>
  }
/>

<ImageAndText
  image="/img/blocks.png"
  docText={
    <>
      #### Blocks Input Plugin Used for managing an array of objects with
      different shapes. Set `ui.compontent` to `"blocks"`. [Plugin
      details](/docs/reference/toolkit/fields/blocks/).
    </>
  }
/>

<ImageAndText
  image="/img/fields/date.jpg"
  docText={
    <>
      #### Date Input Plugin Used for selecting dates with a date picker. Set
      `ui.compontent` to `"date"`. [Plugin
      details](/docs/reference/toolkit/fields/date/).
    </>
  }
/>

Tina also supports some add-on field plugins. These need to be imported and registered from separate packages:

<ImageAndText
  image="/img/fields/markdown.png"
  docText={
    <>
      #### Markdown Input Plugin Used for editing markdown content with a rich
      text editor. Requires additional setup. [Plugin
      details](/docs/reference/toolkit/fields/markdown/).
    </>
  }
/>

<ImageAndText
  image="/img/image.png"
  docText={
    <>
      #### HTML Input Plugin Used for editing HTML content with a rich text
      editor. Requires additional setup. [Plugin
      details](/docs/reference/toolkit/fields/html/).
    </>
  }
/>

## Video Tutorial

Jeff See goes into the step-by-step process of using custom React components in Tina.

<Youtube embedSrc="https://www.youtube.com/embed/amSRwAbgMR0?start=744" />

## Examples

### Custom slider component

Here is a custom slider component that can be used for adjusting image saturation.

![A basic slider custom component](/img/docs/extending-tina/image-saturation.png)

```tsx
import { defineConfig, wrapFieldsWithMeta } from 'tinacms'

//.. other fields
{
  label: "Image Saturation",
  name: "saturation",
  type: "number",
  description: "My custom saturation field",
  ui: {
    parse: (val) => Number(val),

    // wrapping our component in wrapFieldsWithMeta renders our label & description.
    component: wrapFieldsWithMeta(({ field, input, meta }) => {
      return (
        <div>
          <input
            name="saturation"
            id="saturation"
            type="range"
            min="0"
            max="10"
            step=".1"
            // This will pass along props.input.onChange to set our form values as this input changes.
            {...input}
          />
          <br />
          Value: {input.value}
        </div>
      )
    })
  }
}
```
