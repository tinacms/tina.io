---
title: Upgrade to Latest Version
last_edited: '2025-07-11T08:01:15.663Z'
next: content/docs/guides/how-to-update-your-tina-version-with-dependabot.mdx
previous: content/docs/beginner-tutorials/tutorial-overview.mdx
---

To see which version of TinaCMS you’re currently using, check your `package.json` file:

```json
"dependencies": {
  "@tinacms/cli": "^1.x.x",
  "tinacms": "^1.x.x"
}
```

🔍 How to Find Available Versions

> Note: Always update both tinacms and @tinacms/cli to the latest version together to get the latest feature and avoid compatibility issues.

<WarningCallout>
  **Self-Hosted Users:** If you're using the [tina-self-hosted-demo](https://github.com/tinacms/tina-self-hosted-demo) template, **do not upgrade to TinaCMS v3.x** yet. The template currently supports v2.x only. Upgrading to v3.x will cause "Failed loading TinaCMS assets" errors. Use the package versions specified in the template's `package.json`. See the [self-hosted troubleshooting guide](/docs/self-hosted/starters/nextjs-vercel#troubleshooting) for more details.
</WarningCallout>

You can view all published versions of TinaCMS packages on npmjs.com:

[tinacms on npm](https://www.npmjs.com/package/tinacms)

[@tinacms/cli on npm](https://www.npmjs.com/package/@tinacms/cli)

Use your preferred package manager to upgrade TinaCMS to the latest version e.g.

```bash
npm install tinacms @tinacms/cli@latest
```

Example of Installing a Specific Version of TinaCMS with npm:

```bash
npm install tinacms@2.8.0 @tinacms/cli@1.10.0
```

> ⚠️ Reminder: After upgrading Tina, run a dev build to ensure lock file is up to date

```bash
npm run dev
```

This ensures TinaCMS’s internal plugins and dependencies trigger any needed updates to `tina-lock.json`. This file will need to be commited so that TinaCloud knows which version of TinaCMS your site is using.
